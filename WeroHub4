local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Window = Rayfield:CreateWindow({
    Name = "WeroHub - Speed Boost",
    LoadingTitle = "WeroHub",
    LoadingSubtitle = "By WeroScripts",
    ConfigurationSaving = {
        Enabled = false
    },
    Discord = {
        Enabled = false
    },
    KeySystem = false
})

local Tab = Window:CreateTab("Speed", 4483362458)

Tab:CreateToggle({
    Name = "Speed Boost (Speed Coil)",
    CurrentValue = false,
    Flag = "SpeedCoilToggle",
    Callback = function(state)
        local player = game.Players.LocalPlayer
        local backpack = player:WaitForChild("Backpack")
        local humanoid = player.Character and player.Character:FindFirstChildOfClass("Humanoid")

        -- Función para intentar comprar el Speed Coil automáticamente
        local function tryPurchaseSpeedCoil()
            local replicatedStorage = game:GetService("ReplicatedStorage")

            -- Buscar RemoteEvent o función relacionada con la tienda
            for _, v in pairs(replicatedStorage:GetDescendants()) do
                if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then
                    if v.Name:lower():find("buy") or v.Name:lower():find("shop") or v.Name:lower():find("purchase") then
                        pcall(function()
                            v:FireServer("Speed Coil")
                        end)
                    end
                end
            end
        end

        -- Buscar el Speed Coil
        local function findSpeedCoil()
            local tool = backpack:FindFirstChild("Speed Coil")
            if not tool then
                local replicatedStorage = game:GetService("ReplicatedStorage")
                local item = replicatedStorage:FindFirstChild("Speed Coil")
                if item and item:IsA("Tool") then
                    tool = item:Clone()
                    tool.Parent = backpack
                else
                    -- Intentar comprar si no existe
                    tryPurchaseSpeedCoil()
                    task.wait(1) -- esperar a que llegue al Backpack si se compró
                    tool = backpack:FindFirstChild("Speed Coil")
                end
            end
            return tool
        end

        if state then
            local coil = findSpeedCoil()
            if coil and humanoid then
                humanoid:EquipTool(coil)
                task.delay(0.5, function()
                    if humanoid and coil then
                        humanoid:UnequipTools()
                    end
                end)
            end
        else
            if humanoid then
                humanoid:UnequipTools()
            end
        end
    end,
})
